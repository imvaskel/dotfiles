# Dotfiles

My personal dotfiles. For my own use (and yours if you want), to install these, you need GNU stow.

Once installed you can run ``stow --dotfiles -vS .``. Make sure to later set ``STOW_DIR`` somewhere (i.e. in fish/conf.d/ for local)
so stow will read that directory.
